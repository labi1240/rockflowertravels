# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

- Project Setup & Initialization

## Current Goal

- Establish foundational context files and initialize the core Next.js 16 repository.

## Completed

- Context file system (`project-overview.md`, `architecture.md`, `ui-context.md`, `code-standards.md`, `ai-workflow-rules.md`) established.
- **Home page SSR refactor (2026-05-24):** `src/app/page.tsx` is now a Server Component. Modal open/close state lifted into Zustand store `src/store/booking-modal.ts`. Client islands isolated to leaves: `HeroBookingForm`, `ScheduleInteractive`, `RouteMapInteractive`, plus existing client-only components (`Navbar`, `ShuttleTracker`, `BookingModal`, `LightRays`). Hero narrative copy, schedule/map section headers, and "Important travel notes" advisory now ship as server-rendered HTML for SEO. Verified via `curl localhost:3000` — H1 and all marketing copy present in initial response. `tsc --noEmit` clean.
- **Pricing & bundles section (2026-05-26):** Added server-rendered 3-card pricing block inside `ScheduleDashboard.tsx`, sitting between the "Pick your departure" header and the planner. Cards: Single trip ($64.99 CAD), Both lakes bundle ($84.99, sunrise-accent ring), Round-trip circuit ($84.99, primary tone). Each shows price, struck-through unbundled total, and savings note. Prices are marketing display only — checkout still reads `LegTemplate.priceCents` from Prisma; itinerary builder + auto-bundle discount logic is a separate workstream not yet scoped. `tsc --noEmit` clean.
- **Photo-backed service cards added (2026-05-26):** New `src/components/ServiceCards.tsx` (server) + `src/components/ServiceBookButton.tsx` (client-island for modal trigger). Three cards (Sunrise Express, Daytime Circuit, Evening Return) inserted at the top of `ScheduleDashboard` above the pricing block — establishing the three-tier funnel: orient → price → schedule. Each card has photo, eyebrow tag, name, time window, description, 3 highlight bullets, From-price, and a Book CTA that opens the modal with the correct `BookingRouteId`. Real location photos now wired in from `/public/images/locations/`: Sunrise→`moraine-lake-ten-peaks.jpg`, Daytime→`lake-louise-lakeshore.webp`, Evening→`banff.jpg`. User-supplied files were renamed from spaces/special-chars to kebab-case and moved from `/public/` root into `/public/images/locations/` (the original `Lake Louise Lakeshore .webp` had a trailing space before the extension — actively broken). next/image `unoptimized` flag removed so resize + format conversion (AVIF/WebP) kicks in. Unused photos remaining for future Popular Destinations grid: `moraine-lake.jpg`, `samson-mall.jpeg`. Orphaned SVG placeholders at `/public/images/routes/` left in place — safe to delete. `tsc --noEmit` clean.
- **FAQ visual restyle (2026-05-26):** Replaced the single white-box container with 4 separate per-group cards (each `rounded-2xl` with shadow + ring). Each group now leads with an icon chip (sunrise-100 bg) + group name + question count. Toggle changed from small `+` rotating to `×` → larger circular chevron that rotates 180° on open with a stronger color swap (mist → evergreen-800/sunrise-500). Added a sunrise accent stripe on the left edge of the summary row that scales in when an item opens. Header eyebrow upgraded from text-only to a sunrise-bordered pill with a dot ("17 answers, no marketing fluff"). Body text size bumped from 14px → 15px, summary text bumped to 17px on `sm`+. Container width expanded from `max-w-3xl` → `max-w-4xl`. All 17 questions, 6 TODO callouts, and the `SUPPORT_EMAIL` constant unchanged. `tsc --noEmit` clean.
- **FAQ accordion added (2026-05-26):** New `src/components/Faq.tsx` server component inserted between `<RouteMap />` and `<Footer />`. Uses native `<details>`/`<summary>` — no client JS, no Zustand, fully keyboard-accessible by default. 17 questions across 4 groups (Booking & pricing · Schedule · Pickup & logistics · Policies). 11 questions ship with **real answers** derived from the May 03 2026 schedule PDF and the published bundle pricing; the 6 policy questions (cancellation, reschedule, missed bus, pets, accessibility, luggage) render a high-visibility amber "Policy pending — do not ship" callout instead of an answer, so they cannot be shipped accidentally. Support email is a placeholder constant `SUPPORT_EMAIL` at the top of the file. SEO `FAQPage` JSON-LD schema deliberately NOT added — would index the TODO copy. Add schema only once all 17 answers are finalized. `tsc --noEmit` clean.
- **Social proof section added (2026-05-26):** New `src/components/SocialProof.tsx` server component inserted between `<ShuttleTracker />` and `<ScheduleDashboard />` in `src/app/page.tsx`. Contains: eyebrow + headline, 5-column grid mixing 2 rating badges (Google, Tripadvisor — inline brand glyphs as SVG, no asset deps) and 3 operational stat chips, plus 3 testimonial cards in a md:grid-cols-3 layout with star rows and service tag. **All ratings, stats, and testimonials are placeholders** — extracted to `PLACEHOLDER_BADGES` / `PLACEHOLDER_STATS` / `PLACEHOLDER_REVIEWS` constants at the top of the file with a "REPLACE BEFORE LAUNCH" comment banner. Shipping them as-is would be misleading advertising; swap in real Google/Tripadvisor data and verified customer quotes before production. Pure server component, no new deps. `tsc --noEmit` clean.
- **Sunrise Express schedule reverted to PDF (2026-05-26):** The operational PDF (`RockFlower_Daily_Shuttle_Schedule.pdf`, prepared May 03 2026) is canonical and explicitly tags the Moraine→Lakeshore (6:10) and Lakeshore→Samson (6:35) legs as **(positioning)** — non-bookable. Earlier in this session a "catalog" message claimed 6 customer-bookable Sunrise routes; that was reconciled against the PDF and rolled back. `sunriseRoutes` now has 3 entries with a `kind: 'premium' | 'positioning'` discriminator (kept the cleaner per-route `origin`/`dest` fields from the prior refactor — did not regress to the index-based lookup). Only Banff→Moraine is bookable; positioning rows show muted times, "Positioning" badge, and a disabled "Internal" button. Cross-checked Daytime circuit times (C1–C5) and Evening Return (Lakeshore→Banff 6:00–7:15 PM) against the PDF — both already matched, no changes needed. `tsc --noEmit` clean.

- **Admin Operations Center — Phase 1, read-only (2026-06-02):** New `/admin` area. Role gate `src/lib/admin-auth.ts` (`requireAdmin`/`getAdminIdentity`): admin = Clerk `publicMetadata.role === 'admin'` OR primary email in the `ADMIN_EMAILS` env allowlist (comma-separated) — the allowlist is a dev/bootstrap path so the dashboard works before any Clerk metadata is set. `proxy.ts` already forced a session on `/admin(.*)`; the gate adds the role check on top (non-admins → redirect `/`). Shared `src/lib/edmonton.ts` resolves "today" in America/Edmonton as a UTC-midnight Date to match `@db.Date` columns. `src/app/admin/layout.tsx` = gated shell + `_components/AdminNav.tsx` (client, `usePathname` active state). Overview `src/app/admin/page.tsx`: 4 KPI cards (bookings today, revenue today [CONFIRMED only], seats-sold vs capacity, top route) + per-run capacity bars (from `TripLeg` joined to trip/template/route/stops, amber ≥80%/rose ≥100%). Bookings `src/app/admin/bookings/page.tsx`: pure-GET filter form (date / route / status in URL search params, no client JS), table capped at 200 rows, resolves user-or-guest name. **Deferred to Phase 2 (mutations):** live GPS tracking (no telematics feed — rendered as a labeled stub), dynamic pricing toggles, mark sold-out/delayed. `tsc --noEmit` clean (after clearing a stale pre-`/admin` `.next/types` build dir whose `LayoutRoutes="/"` collided with the live dev types — `next build` regenerates it).

- **Admin Phase 1 — Refunds (2026-06-02):** `Booking` gained nullable audit fields `refundedAt`/`refundedBy`/`refundReason` (via `prisma db push` — the live DB is on a db-push workflow, drifted from the single `init` migration, so `migrate dev` would require a destructive reset; pushed additively instead). `src/app/admin/_actions/refunds.ts` `refundBooking({bookingId, reason?})` is admin-gated (`getAdminIdentity`), guards `status===CONFIRMED && payment.status===SUCCEEDED && stripePaymentIntentId`, calls `stripe.refunds.create({payment_intent})`, and writes audit fields only — the existing `charge.refunded` webhook remains the single writer of REFUNDED status. New `src/app/admin/bookings/[reference]/page.tsx` (detail) + `RefundButton.tsx` (client, confirm + reason); bookings table rows now link to it. Full Stripe pay→refund→webhook E2E still needs live test keys to exercise.
- **Admin Phase 2 — DB fare catalog + dynamic pricing + per-fare sale price (2026-06-02):** Pricing moved from the static `src/lib/fares.ts` map into a new Postgres `Fare` table (id = stable slug, `+ active, salePriceCents, saleStartsAt, saleEndsAt, sortOrder`). `fares.ts` is now **types + pure math only**: `quote(fare, passengers, nowMs)` is sale-aware (returns `unitPriceCents`/`originalUnitPriceCents`/`onSale`), plus `isSaleActive`/`effectiveUnitPrice`; `FareId` relaxed to `string`. New `src/lib/fares-db.ts` (`server-only`) `getActiveFares`/`getFareById`/`toFareDTO` (dates→epoch ms `FareDTO`). New client `src/components/FaresProvider.tsx` (`useFares()`) is fed once from the **root layout** (async fetch + graceful empty fallback + request-time `nowMs` to keep SSR/hydration sale eval stable). Rewired `BookingModal`, `HeroBookingForm` (via context), `ServiceCards` (client; computes id/from-price via `tierDefault`/`tierFrom`), and `ScheduleDashboard` (async; price cards fetch DB + sale strikethrough via existing `wasCents` prop). Checkout `create-payment-intent` now reads `getFareById` (rejects null/inactive → 400) and prices via `quote(fare, pax, Date.now())` server-authoritatively. Seed (`prisma/seed.ts`) upserts the 6 fares verbatim so cutover is price-neutral — **parity verified 48/48** (6 fares × pax 1–8, new DB `quote` == original totals). Admin UI: `src/app/admin/fares/page.tsx` (list + `FareActiveToggle`), `fares/[id]/page.tsx` + `FareEditForm.tsx` (base price/toll + sale window with live effective-price preview), actions `src/app/admin/_actions/fares.ts` (`updateFarePricing`/`setFareSale`/`setFareActive`, validated, revalidate `/`+`/admin/fares`). `Fares` nav link added. Home renders DB prices (verified 200). `tsc` clean.

- **Admin Phase 3 — new route + schedule authoring + RouteKind enum relaxation (2026-06-02):** `RouteKind` enum can't grow at runtime, so routes are now identified by a relational `Route.slug` (`@unique`) + `Route.tier`; `Route.kind` made nullable + non-unique (legacy tag for the 3 seeded routes). `Booking` gained `routeName`/`routeSlug` snapshots; `Fare` gained `routeSlug`. Schema applied via `prisma db push --accept-data-loss` (the unique-on-slug warning was benign — all existing rows had NULL slug). Backfill: seed re-keyed route upserts on `slug` (with an updateMany-by-kind guard so existing rows update, not duplicate — verified 3 routes, no dupes) and sets `Fare.routeSlug`; a one-off script backfilled `routeName`/`routeSlug` onto the 10 existing bookings from their `routeKind`. Checkout now writes `routeName` (resolved from `Route.displayName`) + `routeSlug`, and only persists `routeKind` when the fare's value is a real enum member (new routes → null) — **E2E verified: a booking with `routeKind=null` inserts successfully**. Authoring: action `src/app/admin/_actions/routes.ts` (`createStop`, `createRouteWithSchedule` — one transaction creating Route + ScheduleTemplate + LegTemplates + Fare(s), fully validated); admin UI `/admin/routes` (list) + `/admin/routes/new` (`RouteBuilder` client with dynamic legs/fares + `AddStopForm`). New routes are sellable via the existing fare-catalog checkout path immediately. RouteKind-consuming pages updated to prefer `routeName ?? legacyMap[routeKind]`: `admin/page.tsx` (groupBy `routeName`, capacity list uses `route.displayName`), `admin/bookings` (route filter now lists DB routes by slug; display uses `routeName`), `admin/bookings/[reference]`, `my-trips`, `my-trips/[reference]`. `tsc` clean; home/my-trips render (200/307). **DEFERRED to a future Phase 4 — live seat inventory:** new fare-only routes have no `Trip`/`TripLeg` materialization, so they show 0 in the inventory-based occupancy KPIs (checkout still doesn't decrement seats — pre-existing). Promo codes also deferred (sale prices chosen instead).

- **Admin Phase 4 — per-departure seat inventory + oversell prevention (2026-06-02):** Decision: inventory tracked at the **per-departure** grain (`routeSlug + serviceDate + departureTime`), NOT per-`TripLeg` — the seeded legs (Samson circuit) don't align with the Banff-origin fares, and Trip/TripLeg were never materialized (0 rows). New model `DepartureInventory` (`@@unique([routeSlug, serviceDate, departureTime])`, `seatsTotal`/`seatsBooked`); `Booking.seats` added (passenger count snapshot). `src/lib/inventory.ts`: `reserveDepartureSeats` (upsert row at default 25 → atomic conditional `UPDATE … WHERE seatsBooked + n <= seatsTotal` via `$executeRaw`, oversell-safe under concurrency), `releaseDepartureSeats`, `releaseExpiredHolds(scope?)` (conditional status transition so seats release at most once). Checkout reserves seats + creates the booking in one `$transaction` (rolls back together; returns **409** when sold out), sweeps that departure's expired holds first, stores `seats`. Webhook `charge.refunded` releases seats (conditional, idempotent). Cron route `/api/cron/release-holds` (CRON_SECRET-guarded) for global hold expiry — **needs a Vercel cron entry** (`{ "crons": [{ "path": "/api/cron/release-holds", "schedule": "*/5 * * * *" }] }`); not yet added to a vercel config. Admin: `/admin/capacity` (`CapacityManager` — set/add capacity per departure, can't go below booked) + action `setDepartureCapacity`; overview "Capacity by departure" KPI/list repointed from dead TripLeg → DepartureInventory; bookings Seats column now uses `Booking.seats`. Removed the GPS live-tracking stub from the overview (not wanted). **E2E verified:** reserve 2/3 ok → party-of-2 oversell blocked → reserve 1 fills → release works → expired-hold sweep marks EXPIRED + frees seats. `tsc` clean; routes compile; cron returns `{released}`. NOTE: checkout still doesn't create `BookingLeg`/touch `TripLeg` (legacy circuit inventory stays dormant — superseded by DepartureInventory). Dev-server gotcha: restart the dev server after `prisma generate` or the bundled client 500s on new fields.
- **Admin access, demo data & Stripe integration config (2026-06-02):**
  - **Admin access wired for testing:** `ADMIN_EMAILS=lovepreetgill1238@gmail.com` added to `.env.local` (dev allowlist), AND the Clerk user `user_3E2v2ipiizQUwizOfBCzV7NdAO2` granted `publicMetadata.role = "admin"` via the Clerk Backend API (`CLERK_SECRET_KEY`, `sk_test` dev instance). `getAdminIdentity()` passes on either condition; it uses `currentUser()` (fresh fetch) so the role applies without re-login. Confirmed `/admin` renders for that user.
  - **Removable demo seed — `prisma/demo-seed.ts`:** `bun prisma/demo-seed.ts` seeds / `bun prisma/demo-seed.ts clear` removes. Always clears first (idempotent, no dupes). Tagged data: 8 bookings `RF-DEMO-00x` across all 5 statuses (incl. a REFUNDED one with audit fields + a PENDING with a live hold), today's KPIs (4 bookings / $581.62 / seats 38-of-87 / top route Daytime Circuit), 4 `DepartureInventory` rows showing every bar state (empty/amber-80%/full-100%), a sale on `banff-ll` ($65.99→$49.99), `sunrise-banff-ll` toggled hidden (`active:false`), and an admin-authored route `demo-canmore-express` (+CANMORE stop, schedule template, leg, fare `demo-canmore-banff`). `clear` mode also reverts the sale + re-activates the hidden fare. NOTE: seeded bookings carry fake `pi_demo_*` PaymentIntents, so the admin "Issue refund" button errors gracefully on them (real bookings refund for real).
  - **Stripe API version pinned:** `src/lib/stripe.ts` now `new Stripe(key, { apiVersion: '2026-04-22.dahlia', typescript: true })` — matches the installed `stripe` v22.1.1 SDK types (was inheriting the account default `2024-04-10`). Affects outgoing API calls only; webhook payload version is still the account default (fine — we read stable fields). `tsc` clean.
  - **Local webhook testing wired:** `STRIPE_WEBHOOK_SECRET` in `.env.local` updated to match the `stripe listen` secret (the CLI upgrade 1.33→1.42.1 had rotated it), so `payment_intent.succeeded` / `charge.refunded` verify locally and flip booking status + release seats. Run `stripe listen --forward-to localhost:3000/api/webhooks/stripe` during local testing. Reminder: deployed env uses a different (Dashboard/Vercel) webhook secret.
  - **Package manager:** project uses **bun** (`bun run dev`, `bunx …`), not npm.

- **Code-review remediation pass — CodeRabbit 38 findings (2026-06-02):** Triaged the full review; fixed 25 valid items, skipped 13 (verified non-issues). Notable fixes: **correctness** — `setDepartureCapacity` race closed (read-then-upsert → `createMany(skipDuplicates)` + atomic conditional `updateMany WHERE seatsBooked <= seatsTotal`); refund now passes a deterministic Stripe `idempotencyKey` (`refund_${pi}`) and revalidates the detail page by **reference** (route is `/admin/bookings/[reference]`, was using bookingId); admin overview date label was rendering the **previous** day (UTC-midnight `today` formatted in `EDMONTON_TZ`) → now formatted in UTC; stripe `charge.refunded` skips seat release + warns when no route identity (was passing `''`, silent no-op); `BookingModal` syncs route state to the fallback fare so checkout can't submit a stale id; `createRouteWithSchedule` now validates fare origin/destination. **Robustness/UX** — `RouteBuilder` uses stable `rowId` keys (was array index) + client HH:MM validation + aria-labels; `FareActiveToggle` surfaces action errors; `AddStopForm` is a real `<form>` (Enter submits); `HeroBookingForm` initializes route from the live catalog. **Minor** — timing-safe CRON bearer compare; `ADMIN_EMAILS` parsed once at module scope; `FaresProvider` warns on unexpected tier; removed unsafe `salePriceCents as number` cast + redundant `isFareId`; renamed a shadowed `route` var in checkout; demo-seed `banff` null guard. **Schema:** added `@@index([routeSlug])` to `Fare` — **applied to the live DB via `prisma db push`** (migrate dev still wants a destructive reset due to the long-standing db-push drift; pushed additively). Verified: index `Fare_routeSlug_idx` present in Postgres; `tsc --noEmit`, `prisma validate`, and `eslint` all clean. **Skipped (non-issues):** `byTier`/`tierFrom` undefined guards (provider always populates all 3 tier keys; empty tiers filtered upstream), `Fare.routeKind` "enum" finding (it's typed `String`), `serviceDate.toISOString().slice(0,10)` (column is `@db.Date`, UTC-midnight), datetime-local Edmonton-TZ sale parsing (no helper supports time-of-day — needs new TZ infra), `.claude/settings.json` npm/git-commit permissions (intentional), ServiceCards editorial price strings (documented choice).

## In Progress

- None — admin write-features complete across Phases 1–4 (refunds, dynamic pricing + sale prices, new-route authoring, per-departure seat inventory with oversell prevention). Follow-ups if needed: add the Vercel cron entry for `/api/cron/release-holds`; reconcile the db-push drift into a clean migration before production; optionally pre-provision DepartureInventory capacity for known departures via the seed.

## Next Up

- Unit 01: Initialize Next.js 16 App Router repository with Tailwind 4.0 and shadcn/ui.
- Unit 02: Configure Clerk authentication with `proxy.ts` (Node.js runtime).
- Unit 03: Set up Prisma schema and connect to PostgreSQL database.

## Open Questions

- Which specific shadcn/ui components will be required for the first feature build?
- What are the initial data sources to be ingested for the Ammo Terminal aggregator functionality?

## Architecture Decisions

- **Framework:** Next.js 16 utilizing async Request-time APIs exclusively to prevent application crashes.
- **Routing/Auth:** Clerk implemented via `proxy.ts` (Node.js runtime) rather than the deprecated `middleware.ts`.
- **State Management:** Strict segregation between server data fetching (TanStack Query) and global client UI state (Zustand).
- **Styling:** Tailwind 4.0 with HSL custom properties to allow predictable lightness and saturation adjustments.

## Session Notes

- **2026-06-01 — Theme shift: green-dominant → cream-dominant (Refactoring UI 60/30/10).**
  - `globals.css`: retuned the `mist` ramp from cool-grey to a warm cream/charcoal neutral (token name kept so existing utilities resolve); added `mist-950`; bound shadcn `:root` tokens + `--bg`/`--text` to the cream/evergreen/sunrise ramps; warmed two-layer shadows; cards lift off the cream page via a near-white warm surface.
  - Palette roles: cream neutrals = dominant 60% (backgrounds/text), `evergreen` green = 10% accent (titles, primary buttons, active states), `sunrise` gold kept as secondary accent (CTAs, premium Sunrise Express run).
  - Converted all major surfaces off the dark-green slabs to cream across Hero (cream scrim over the video — opaque left for dark text + green title, fading right to reveal the mountains), HeroBookingForm (white card to pop on the cream hero), Navbar (dark `main_logo` throughout since the bar now sits on cream), Footer, Schedule, RouteMap, ShuttleTracker, ServiceCards, SocialProof, Faq, BookingModal, my-trips, privacy-policy.
  - Map route-line/marker paint colors and the dark photo-overlay text in ServiceCards intentionally left unchanged.
  - Known pre-existing (out of scope): `src/app/my-trips/page.tsx` `BookingRow` vs Prisma `Booking` type mismatch.
- **2026-06-01 — Route-specific pricing restructure (client June pricing sheet).**
  - Root cause of "price looks the same regardless of where you go": every surface hardcoded a flat $64.99 keyed only by 3 route IDs — no origin→destination fare concept.
  - New single source of truth: `src/lib/fares.ts` — 6 fare products across 3 tiers, with `quote()` (fare + per-passenger toll, then 5% GST on the combined subtotal). `FareId` flows through the store (`BookingRouteId` is now an alias), the checkout API (server-authoritative `getFare`/`quote`), BookingModal (grouped selector + live toll/GST breakdown), HeroBookingForm, ServiceCards, ScheduleInteractive, ScheduleDashboard, Faq, and the seed.
  - Prices: Sunrise Banff→Moraine $99.98, Sunrise Banff→LL $79.99, Banff→LL $65.99, Banff→LL+Moraine $89.99 (+$5 Moraine toll/guest), Lake Louise⇄Moraine $89.99 (round trip). Decisions confirmed with client: daytime now originates in Banff, toll is per-passenger, GST applies to fare+toll.
  - ASSUMPTION (flagged in code): Evening Return (LL→Banff) kept at $65.99 mirroring Banff→LL — not on the sheet; confirm before launch.
  - NOTE: ScheduleInteractive still shows the operational Samson-Mall circuit timetable (unchanged times); only pricing/products were restructured. Booking `subtotalCents` folds the toll in (no separate DB toll column).
  - Verified: `tsc --noEmit` clean for all touched files (pre-existing my-trips `BookingRow` error unchanged).
- Context files are complete and locked in. 
- Ready to begin spec-driven development. 
- Ensure the first prompt instructs the AI to read the context files and `Unit 01` spec before executing any terminal commands or generating code.
- **2026-06-02 — SEO foundation pass (technical + on-page).**
  - New single source of truth: `src/lib/seo.ts` — site identity (name, URL, description, keyword set), `absoluteUrl()` helper, and schema.org JSON-LD builders (`organizationSchema` as `TravelAgency`, `websiteSchema`, `faqSchema`). Base URL reads `NEXT_PUBLIC_SITE_URL` and falls back to `https://rockflowertravels.ca`.
  - FAQ structured data is built from ONLY the 11 confirmed answers in `Faq.tsx`; the 6 `todo` "Policy pending" questions are intentionally excluded so placeholder text is never indexed. Keep `FAQ_ENTRIES` in sync when answers are finalized.
  - Metadata routes added: `app/robots.ts` (allow public, disallow `/admin` `/my-trips` `/sign-in` `/sign-up` `/api/`, sitemap ref), `app/sitemap.ts` (home + privacy only), `app/manifest.ts` (PWA), `app/opengraph-image.tsx` (dynamic 1200×630 branded card via `next/og`).
  - Root `layout.tsx`: upgraded `metadata` (title template `%s | RockFlower Travels`, keywords, authors/creator/publisher, `robots` w/ googleBot directives, icons, Twitter `summary_large_image`, OG); added `viewport` export (themeColor, colorScheme light); injects Organization + WebSite JSON-LD site-wide via new `components/JsonLd.tsx`. Home page injects FAQPage JSON-LD.
  - Private/auth pages set `robots: { index:false, follow:false }`: `sign-in`, `sign-up`, `my-trips`, `my-trips/[reference]`, `admin/layout`. Privacy page got an explicit canonical.
  - Verified: `tsc --noEmit` exit 0.
  - FOLLOW-UP before launch: set `NEXT_PUBLIC_SITE_URL` per env on Vercel; add a properly-sized favicon/apple-touch icon (currently reusing `/main_logo.png`, 2954×1440); add search-console `verification` tokens to root metadata; add FAQPage entries for the policy questions once answered.
- **2026-06-02 — Boarding pass: real QR + ticket save/share + Apple Wallet scaffold.**
  - Replaced the purely decorative fake-barcode strip in `BookingModal.tsx` `BoardingPass` (90 `<span>` bars keyed on `i%3`/`i%2`, encoded nothing) with a real `QRCodeSVG` (`qrcode.react@4.2.0`, `level="M"`) encoding `absoluteUrl(/my-trips/<ref>)` — scans to the live booking page.
  - New `ConfirmedTicket` wrapper owns a capture ref around `BoardingPass`; new `TicketActions` toolbar adds 4 buttons: **Add to calendar** (`.ics`, zero-dep, embedded `VTIMEZONE` America/Edmonton + `VALARM` -1h), **Save as PDF** (opens print window with a captured PNG → `window.print()`), **Save image** (PNG download), **Copy link** (clipboard). PNG/print use `html-to-image@1.11.13` (`toPng`) — the one added dep. Print window opened synchronously inside the click gesture to dodge popup blockers.
  - Apple Wallet: deferred (no Apple Developer account yet). Scaffolded `src/app/api/wallet/apple/[reference]/route.ts` — real auth + ownership + CONFIRMED checks, returns 501 with full Phase-B impl notes (Pass Type ID cert, `passkit-generator`, env secrets, `PKBarcodeFormatQR` matching the on-screen QR). "Add to Apple Wallet" button in BookingModal gated behind `NEXT_PUBLIC_WALLET_ENABLED` (off).
  - Verified: `tsc --noEmit` exit 0, eslint clean. **Browser-verified (Playwright, headless)**: booking funnel Route→Contact→Payment renders with 0 console errors and the Stripe Payment Element mounts; the confirmed boarding pass renders a real QR (replacing the fake bars). `html-to-image` `toPng` produces a valid `data:image/png;base64` (~147KB) — confirms Tailwind 4 oklch + SVG QR capture works. The `.ics` is a valid VCALENDAR (correct `DTSTART;TZID=America/Edmonton:20260521T070000`, embedded MDT/MST VTIMEZONE, `DURATION:PT2H`, `VALARM -PT1H`, live booking URL in DESCRIPTION). SEO routes (`/robots.txt`, `/sitemap.xml`, `/manifest.webmanifest`, `/opengraph-image`) all 200; homepage head carries title/keywords/canonical/OG(+1200×630 image)/Twitter + JSON-LD (TravelAgency, WebSite, FAQPage×11). Verified via a temporary `/dev/ticket-preview` harness that has since been deleted.
